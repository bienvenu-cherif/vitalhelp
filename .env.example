# .env.example — Modèle pour les nouveaux développeurs
# Copie ce fichier en .env et remplis les vraies valeurs

# === Base de données PostgreSQL ===
DB_USER=postgres
DB_PASSWORD=ton_mot_de_passe
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vitalhelp

# === En production (Render) : DATABASE_URL prend le pas sur DB_* ===
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# === Flask ===
SECRET_KEY=genere_avec_python_-c_import_secrets_secrets.token_hex_32

# === IA Groq ===
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxx

# === Environnement (development ou production) ===
FLASK_ENV=development